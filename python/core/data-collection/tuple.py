"""
Tuple : immutable, ordered, indexed, slicing, duplicate values are allowed

# syntax:
tuple_obj = tuple()
tuple_obj = ()
tuple_obj = 1,
"""

# Creating a tuple

tuple_obj = (1, 2, 3, 4, 5)
comma_obj = 1,2,3,4,5
# print(type(comma_obj))
# print(len(comma_obj))
# print(tuple_obj)

# print(dir(comma_obj))

comma_obj = 1,2,3,4,5
# print(comma_obj[0])
# print(comma_obj[::-1])

# comma_obj[0] = 11 # TypeError: 'tuple' object does not support item assignment

# tokens = list(range(1, 11))
# tokens.pop(0)
# tokens = tuple(tokens)
# tokens.insert(0, 1)
# print(tokens)